# MazeRunnerCV
# OpenCV - Python

Implementation of a maze game in Python that uses the real-time image of the WebCam to decide the next move, through a JoyStick drawn on the WebCam.

To try it, just run:
```
python3 maze.py
```
